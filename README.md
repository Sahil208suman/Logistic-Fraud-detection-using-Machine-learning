# Logistic-Fraud-detection-using-Machine-learning
The system uses supervised learning techniques to train on historical transaction data. After preprocessing and handling class imbalance, multiple classification algorithms were applied. The best-performing model was selected based on recall, as fraud detection requires minimizing false negatives while maintaining acceptable false positives.
